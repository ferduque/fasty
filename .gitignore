# dev workspace
.worktrees/
.DS_Store

# env files (Supabase URL + anon key live in src/public-config.js instead)
.env*
!.env.example

# wrangler / Cloudflare local state
.wrangler
.dev.vars*
!.dev.vars.example
