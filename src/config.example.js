/**
 * Supabase project config.
 *
 * Copy this file to `src/config.js` (which is gitignored) and fill in your
 * project's URL + anon public key. The anon key is intended to be public —
 * Row-Level Security in the database is what protects user data.
 *
 * Get these values from your Supabase dashboard at
 *   Settings → API → Project URL  /  Project API keys → anon public
 */
export const SUPABASE_URL = 'https://YOUR_PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_ANON_PUBLIC_KEY';
